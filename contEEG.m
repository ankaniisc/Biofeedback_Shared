%  PLOT BASELINE RAW POWER SPECTRUM AND LOG POWERSPECTRUM
%% clear all variables to begin with and init

function state = contEEG(state,hStart,hStop,hCalibrate,hRun,handles)
    
    switch state
        case 1
            %% only monitoe the eeg and plot the 
            disp('negative one')
        case 3
            disp('zero')
        case 4
            disp('positive one')        
    end
  
end 

